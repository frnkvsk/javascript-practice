// import { createSlice } from '@reduxjs/toolkit';

// export const editMemeDataSlice = createSlice({
//   name: 'editMemeData',
//   initialState: {},
//   reducers: {
//     setEditMemeData: (state, action) => {
//       if(action.id) {
//         state.id = action.payload.id;
//         state.img = action.payload.img;
//         state.top = action.payload.top;
//         state.bottom = action.payload.bottom;
//       } else {
//         state = {};
//       }
//     },
//   },
// });

// export const { editMemeData } = editMemeSlice.actions;

// export const selectEditMemeData = state => state.editMemeData;

// export default editMemeData.reducer;
